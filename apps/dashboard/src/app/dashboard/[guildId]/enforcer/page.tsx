import { ComingSoonPage } from '../../../../components/coming-soon-page';

export default function EnforcerPage() {
  return (
    <ComingSoonPage
      title="Enforcer"
      description="Policy editor, flag queue with decisions, and the bookkeeping ledger with search, filter, and export."
    />
  );
}
