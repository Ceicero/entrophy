# dependencies
node_modules

# env
.env
.env.*.local

# build outputs
dist
.next
out
coverage
playwright-report
test-results

# logs
*.log

# OS
.DS_Store

# tooling caches
.turbo
*.tsbuildinfo

# apps/dashboard/next-env.d.ts is generated by Next.js but we keep it tracked
# so `tsc`/editors have a stable reference without needing a dev-server run first.
